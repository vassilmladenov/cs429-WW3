public class City
{
    public City(string name, int points)
    {
        this.Name = name;
        this.Points = points;
    }

    public string Name { get; }

    public int Points { get; }
}